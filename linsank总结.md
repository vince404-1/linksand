# “林砂”智能对话代理系统 — 项目报告

## 一、项目概述

“林砂”是一个**部署于微信生态**的智能对话代理系统。  
与传统“一问一答”型机器人不同，它实现了**多轮有状态对话**，通过**多个专职AI Agent（代理）协作**完成：
- 上下文总结
- 引导方式统计与选择
- 内容安全审查
- 角色化回复生成

同时，系统具备**语音识别、配额管理、异步消息处理、全量数据持久化**等完整工程能力，所有对话轨迹及中间推理产物均存储于腾讯云COS，形成可审计、可优化的数据资产。

---

## 二、核心功能模块

### 1. 多模态消息接入
- **文本消息**：直接进入对话流
- **语音消息**：自动完成下载 → 转码（AMR→MP3）→ 微信语音识别 → 文本转换，无缝融入对话
- **事件处理**：关注事件自动回复欢迎图片

### 2. 多Agent推理引擎（核心）
每次对话依次调用以下5个Agent（模型：`qwen-turbo`）：
| Agent | 职责 | 关键能力 |
|-------|------|----------|
| **Summarizer** | 总结历史对话 | 提炼70%用户内容 + 30%助手反馈，防止上下文溢出 |
| **Statistician** | 更新引导方式统计表 | 记录A-G各类策略的使用次数，为决策提供数据 |
| **Sensor** | 内容安全审查 | 过滤黄赌毒、政治、宗教等违规内容 |
| **Selecter** | 动态选择回复策略 | 综合总结、统计、规则，从A-G中选择最优引导方式 |
| **Responder** | 生成最终回复 | 严格遵循选中策略，生成口语化、字数合规的回复 |

### 3. 异步消息与可靠性保障
- 接收服务立即返回 `success` 给微信服务器，避免超时
- 后台守护线程或定时任务负责转发、推理、回复发送

### 4. 用户配额与生命周期管理
- 每位用户独立配额，可动态更新
- 超过配额的请求会被静默拦截
- 可通过特殊密钥无限激活新用户

### 5. 全量数据资产存储
- 对话历史、中间状态、语音文件、日志等均分类存储于COS
- 支持按用户ID检索完整交互链路

---

## 三、系统架构图（字符版）

┌─────────────┐
│ 微信用户 │
└──────┬──────┘
│ 消息/语音/事件
▼
┌─────────────────────────┐
│ 微信公众平台服务器 │
└────────────┬────────────┘
│ POST XML (带签名)
▼
┌────────────────────────────────────────────────────────┐
│ 服务1：消息接收与预处理器 (flask_sandLink_Receive_1) │
│ app.py /wechatrevice │
│ - 验证签名 (GET) / 接收XML (POST) │
│ - 立即返回"success"给微信 │
│ - 启动守护线程异步转发至推理服务 │
└────────────┬───────────────────────────────────────────┘
│ 异步转发 (text/xml)
▼
┌────────────────────────────────────────────────────────┐
│ 服务2：核心推理引擎 (flask_sandLink_Recorder + API) │
│ app.py /inference │
│ functionSetInference.py 多Agent链 │
│ - 从COS读取用户历史模板 │
│ - 执行 Summarizer → Statistician → Sensor → │
│ Selecter → Responder │
│ - 返回最终回复、状态、成本 │
│ - 将新状态写回COS │
└────────────┬───────────────────────────────────────────┘
│ 返回JSON (content, cost, qwenTemp)
▼
┌────────────────────────────────────────────────────────┐
│ 服务3：定时调度与批量推送器 (flask_sandLink_Responser) │
│ index.py main_handler (定时触发) │
│ - 从COS收集待处理消息日志 │
│ - 合并、去重、计算时间差，生成待回复列表 │
│ - 批量并发请求服务2的/inference接口 │
│ - 调用微信客服消息接口发送回复 │
│ - 更新配额、移动过期文件、记录聊天历史 │
└────────────────────────────────────────────────────────┘
▲ ▲
│ 读写历史、日志 │ 读写状态、配额、语音文件
▼ ▼
┌────────────────────────────────────────────────────────┐
│ 腾讯云 COS 对象存储 │
│ user-cht-his-1-13 │
│ ├── tpFloder1/linkSand/qwen_log/ (用户对话模板) │
│ ├── userMessage_log/ (原始消息日志) │
│ ├── wx_user_v1/ (用户文本备份) │
│ ├── chat_history/ (聊天记录) │
│ ├── waittingList_plDf/ (待处理消息df) │
│ ├── wx_voice/ (语音文件) │
│ └── wx_config/Quota.json (用户配额) │
└────────────────────────────────────────────────────────┘


### 数据流简要说明：
1. **实时路径**：用户消息 → 微信 → 服务1(接收) → 异步 → 服务2(推理) → COS（状态持久化）  
2. **批量路径**：服务3(定时器) → 从COS拉取消息日志 → 并发服务2 → 调用微信接口发送回复 → 更新COS配额/状态/聊天记录  

---

## 四、技术栈

| 层级 | 技术/平台 | 说明 |
|------|-----------|------|
| 后端框架 | Python + Flask | 提供 HTTP API 和微信回调处理 |
| AI 模型 | 阿里云百炼 `qwen-turbo` | 支持 `thinking` (思维链) 模式，用于全部5个Agent |
| 云平台 | 腾讯云 SCF (云函数) | 无服务器计算，按需弹性伸缩，承载所有服务 |
| 存储 | 腾讯云 COS (对象存储) | 存储对话模板、日志、语音、配额、parquet 文件等 |
| 外部接口 | 微信公众号接口 | 消息接收/发送、客服消息、语音识别、临时素材上传 |
| 数据处理 | `polars` DataFrame | 用于高效处理用户消息日志的合并、时间差计算等 |
| 音频处理 | FFmpeg | AMR/Speex 转 MP3 |
| 异步处理 | `threading` / `asyncio` | 消息异步转发、微信并发发送 |

---

## 五、项目亮点

### 🌟 1. 多Agent协作的链式推理架构
将复杂的对话任务拆解为五个专职Agent，每个Agent拥有明确的输入、输出和性能指标。相比单次大Prompt，这种设计：
- 可解释性强，便于调试
- 每个环节可独立优化或替换模型
- 成本精细可控，每个Agent调用均单独计算费用

### 🌟 2. 有状态的长期对话管理
通过 JSON 对话模板，系统完整保存了：
- 历史输入/输出
- 每一次的总结、统计、审查结果
- 策略选择记录

使得对话不仅是“记得内容”，更“记得为什么这样回复”，为实现真正的个性化交互奠定基础。

### 🌟 3. 成本感知与优化设计
系统内置 `calculate_cost` 函数，根据输入/输出/思考 token 精确计算每次推理成本。运维人员可以清晰了解每位用户、每种策略的开销，为商业化运营提供决策依据。

### 🌟 4. 高度解耦的异步事件驱动架构
将消息**接收、推理、调度发送**三大职责解耦为三个独立服务：
- 接收服务保证微信回调及时响应，不丢失消息
- 推理服务专注计算，可水平扩展
- 调度服务负责聚合、批量处理，减少API调用次数，并处理速率限制

### 🌟 5. 完备的数据资产管理
所有原始消息、语音文件、语音识别文本、回复内容及中间推理状态均结构化存储于COS。这份持续增长的数据资产可用于：
- 用户画像分析
- 引导策略效果评估
- 微调垂直领域模型
- 对话质量审计与合规审查

---

## 六、各模块伪代码核心逻辑摘要

### 6.1 消息接收器 (flask_sandLink_Receive_1/app.py)
- GET：微信签名验证，返回 `echostr`
- POST：接收XML，立即返回 `"success"`，同时开启线程转发XML到推理API

### 6.2 推理引擎 (functionSetInference.py)
- `read_tenmpleat(user_id)`：从COS读取用户历史模板
- `linsank_inference(data_, content_)`：
  1. Summarizer (conversation_count>0时)
  2. Statistician (conversation_count>0时)
  3. Sensor (内容审查，返回 "F" 或 "null")
  4. Selecter (若未违规，选择引导方式A-G)
  5. Responder (生成最终回复)
  累计成本，更新状态并返回
- `calculate_cost()`：精确到百万token的计费

### 6.3 录音与语音处理 (flask_sandLink_Recorder/)
- 下载微信语音 → 转码 → 语音识别 → 存为JSON
- 所有文本消息实时保存到COS日志和数据备份

### 6.4 定时批量处理 (flask_sandLink_Responser/)
- `main_handler` 由腾讯云SCF定时触发
- 从COS拉取分散的消息日志，合并为 `polars` DataFrame
- 筛选出超过180秒未回复且配额充足的用户
- 并发调用推理API，获取回复
- 通过微信客服接口发送，扣减配额，移动过期文件

---

## 七、总结

“林砂”项目已经不仅仅是一个AI聊天原型，而是一个具有**生产级可靠性、成本可度量、状态可追溯**的完整对话服务系统。  
其多Agent协作架构为复杂对话策略提供了工程化基础，Serverless + COS 的组合保证了弹性与低成本，全量数据沉淀则打开了后续精细化运营和模型迭代的大门。

---

> *本报告基于提供的全部伪代码文件：*  
> `flask_sandLink_Receive_1 app.py`、`functionSetInference.py`、`flask_sandLink_Recorder app.py`、`functionSetOne.py`、`flask_sandLink_Responser index.py`、`functionSetResponser.py`